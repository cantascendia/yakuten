---
title: "Annuaire médical"
description: "Un annuaire structuré de ressources d'accès aux soins remontées par la communauté. Vérifiez toujours directement avant de vous déplacer."
evidenceLevel: X
lastReviewed: 2026-04-09
references: []
pagefind: false
---

import HospitalCard from '../../../../components/ui/HospitalCard.astro';
import WarningBox from '../../../../components/ui/WarningBox.astro';
import DangerBox from '../../../../components/ui/DangerBox.astro';
import GlassCard from '../../../../components/ui/GlassCard.astro';

import hospitalsRaw from '../../../../data/hospitals.json';

export const hospitals = hospitalsRaw;
export const cityOrder = ['北京', '上海', '广州', '深圳', '杭州', '南京', '武汉', '成都', '重庆', '西安', '沈阳', '香港', '台北'];
export const hospitalsByCity = cityOrder
  .map((city) => ({
    city,
    hospitals: hospitals.filter((hospital) => hospital.city === city),
  }))
  .filter(({ hospitals: cityHospitals }) => cityHospitals.length > 0);
export const cityCount = hospitalsByCity.length;

<WarningBox title="Vérifiez avant de vous déplacer">
Ces fiches sont des notes d'accès tenues à jour par la communauté, et non des garanties officielles émanant des établissements. Les politiques, les services et la disponibilité des médecins peuvent évoluer du jour au lendemain : une confirmation directe reste donc toujours indispensable.
</WarningBox>

Cet annuaire recense pour l'instant {hospitals.length} fiches remontées par la communauté, réparties sur {cityCount} villes. À ce stade, les données sources sont encore majoritairement en chinois ; les noms d'établissements et les notes locales peuvent donc s'afficher en chinois.

---

{hospitalsByCity.map(({ city, hospitals: cityHospitals }) => (
  <>
    <h2>{city}</h2>
    {cityHospitals.map((hospital) => <HospitalCard hospital={hospital} />)}
    <hr />
  </>
))}

---

<GlassCard>

**Comment utiliser cette page**

1. Considérez-la comme un point de départ pour mener vos propres vérifications.
2. Confirmez directement auprès de l'établissement le service concerné, la disponibilité des médecins et les modalités d'inscription.
3. Associez l'accès à un soignant à la planification d'un bilan biologique de référence et à une vigilance face aux urgences.

</GlassCard>

<DangerBox title="Note relative aux urgences">
Cet annuaire n'est pas un service d'orientation en urgence. En cas de symptômes urgents, rendez-vous d'abord aux urgences plutôt que de vous en remettre à la consultation de cet annuaire.
</DangerBox>
