---
title: "Directorio médico"
description: "Un directorio de recursos de acceso a la atención médica, recopilados y reportados por la comunidad. Confirma siempre directamente antes de acudir."
evidenceLevel: X
lastReviewed: 2026-04-09
references: []
pagefind: false
---

import HospitalCard from '../../../../components/ui/HospitalCard.astro';
import WarningBox from '../../../../components/ui/WarningBox.astro';
import DangerBox from '../../../../components/ui/DangerBox.astro';
import GlassCard from '../../../../components/ui/GlassCard.astro';

import hospitalsRaw from '../../../../data/hospitals.json';

export const hospitals = hospitalsRaw;
export const cityOrder = ['北京', '上海', '广州', '深圳', '杭州', '南京', '武汉', '成都', '重庆', '西安', '沈阳', '香港', '台北'];
export const hospitalsByCity = cityOrder
  .map((city) => ({
    city,
    hospitals: hospitals.filter((hospital) => hospital.city === city),
  }))
  .filter(({ hospitals: cityHospitals }) => cityHospitals.length > 0);
export const cityCount = hospitalsByCity.length;

<WarningBox title="Confirma antes de acudir">
Estos listados son notas de acceso que mantiene la comunidad, no garantías oficiales de las instituciones. Las políticas, los servicios y la disponibilidad de médicos pueden cambiar de un día para otro, así que confirmar directamente con el centro es siempre imprescindible.
</WarningBox>

Por ahora este directorio reúne {hospitals.length} entradas aportadas por la comunidad en {cityCount} ciudades. Los datos de origen siguen estando, en esta etapa, mayormente en chino, de modo que es posible que los nombres de los hospitales y las notas locales aún aparezcan en ese idioma.

---

{hospitalsByCity.map(({ city, hospitals: cityHospitals }) => (
  <>
    <h2>{city}</h2>
    {cityHospitals.map((hospital) => <HospitalCard hospital={hospital} />)}
    <hr />
  </>
))}

---

<GlassCard>

**Cómo aprovechar esta página**

1. Tómala como punto de partida para tu propia comprobación.
2. Confirma directamente con el centro el servicio, la disponibilidad de médicos y las reglas para sacar cita.
3. Combina el acceso a un proveedor con la planificación de tus análisis de base y con estar preparada ante una emergencia.

</GlassCard>

<DangerBox title="Nota sobre emergencias">
Este directorio no es un servicio de derivación de urgencias. Si tienes síntomas urgentes, acude antes que nada a un servicio de emergencias en lugar de fiarte de lo que consultes aquí.
</DangerBox>
