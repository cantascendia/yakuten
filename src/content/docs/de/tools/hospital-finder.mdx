---
title: "Medizinisches Verzeichnis"
description: "Ein strukturiertes Verzeichnis von Versorgungsangeboten auf Grundlage von Community-Berichten. Bitte vor jedem Besuch direkt überprüfen."
evidenceLevel: X
lastReviewed: 2026-04-09
references: []
pagefind: false
---

import HospitalCard from '../../../../components/ui/HospitalCard.astro';
import WarningBox from '../../../../components/ui/WarningBox.astro';
import DangerBox from '../../../../components/ui/DangerBox.astro';
import GlassCard from '../../../../components/ui/GlassCard.astro';

import hospitalsRaw from '../../../../data/hospitals.json';

export const hospitals = hospitalsRaw;
export const cityOrder = ['北京', '上海', '广州', '深圳', '杭州', '南京', '武汉', '成都', '重庆', '西安', '沈阳', '香港', '台北'];
export const hospitalsByCity = cityOrder
  .map((city) => ({
    city,
    hospitals: hospitals.filter((hospital) => hospital.city === city),
  }))
  .filter(({ hospitals: cityHospitals }) => cityHospitals.length > 0);
export const cityCount = hospitalsByCity.length;
export const shownCount = hospitalsByCity.reduce((n, group) => n + group.hospitals.length, 0);

<WarningBox title="Vor dem Besuch überprüfen">
Diese Einträge sind von der Community gepflegte Hinweise zum Versorgungszugang – keine offiziellen Zusagen der Einrichtungen. Vorgaben, Abteilungen und die Verfügbarkeit von Ärztinnen und Ärzten können sich rasch ändern; eine direkte Rückfrage ist deshalb immer notwendig.
</WarningBox>

Dieses Verzeichnis enthält derzeit {shownCount} von der Community gemeldete Einträge in {cityCount} Städten. Die zugrunde liegenden Quelldaten liegen in diesem Stadium noch überwiegend auf Chinesisch vor, weshalb Klinikbezeichnungen und örtliche Hinweise weiterhin auf Chinesisch erscheinen können.

---

{hospitalsByCity.map(({ city, hospitals: cityHospitals }) => (
  <>
    <h2>{city}</h2>
    {cityHospitals.map((hospital) => <HospitalCard hospital={hospital} />)}
    <hr />
  </>
))}

---

<GlassCard>

**So nutzt du diese Seite**

1. Nutze sie als Ausgangspunkt für deine eigene Überprüfung.
2. Kläre Abteilung, Verfügbarkeit der Ärztinnen und Ärzte sowie die Anmeldemodalitäten direkt mit der Einrichtung ab.
3. Verbinde den Zugang zu einer Versorgungsstelle mit der Planung deiner Ausgangs-Laborwerte und einem wachen Bewusstsein für Notfälle.

</GlassCard>

<DangerBox title="Hinweis für Notfälle">
Dieses Verzeichnis ist kein Notfall-Vermittlungsdienst. Wenn du akute Symptome hast, suche zuerst die Notfallversorgung auf, statt dich auf das Durchsuchen des Verzeichnisses zu verlassen.
</DangerBox>
