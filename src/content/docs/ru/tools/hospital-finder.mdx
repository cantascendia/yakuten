---
title: "Медицинский справочник"
description: "Структурированный справочник ресурсов по доступу к медицинской помощи, составленный на основе сообщений сообщества. Уточняйте информацию напрямую перед визитом."
evidenceLevel: X
lastReviewed: 2026-04-09
references: []
pagefind: false
noindex: true
---

import HospitalCard from '../../../../components/ui/HospitalCard.astro';
import WarningBox from '../../../../components/ui/WarningBox.astro';
import DangerBox from '../../../../components/ui/DangerBox.astro';
import GlassCard from '../../../../components/ui/GlassCard.astro';

import hospitalsRaw from '../../../../data/hospitals.json';

export const hospitals = hospitalsRaw;
export const cityOrder = ['北京', '上海', '广州', '深圳', '杭州', '南京', '武汉', '成都', '重庆', '西安', '沈阳', '香港', '台北'];
export const hospitalsByCity = cityOrder
  .map((city) => ({
    city,
    hospitals: hospitals.filter((hospital) => hospital.city === city),
  }))
  .filter(({ hospitals: cityHospitals }) => cityHospitals.length > 0);
export const cityCount = hospitalsByCity.length;

<WarningBox title="Уточняйте информацию перед визитом">
Эти записи представляют собой заметки о доступе к помощи, поддерживаемые сообществом, а не официальные гарантии учреждений. Регламенты, отделения и наличие конкретных врачей могут быстро меняться, поэтому прямое подтверждение всегда необходимо.
</WarningBox>

В настоящее время справочник включает {hospitals.length} записей, сообщённых сообществом, по {cityCount} городам. Исходные данные на данном этапе остаются преимущественно на китайском языке, поэтому названия больниц и местные заметки могут по-прежнему отображаться на китайском.

---

{hospitalsByCity.map(({ city, hospitals: cityHospitals }) => (
  <>
    <h2>{city}</h2>
    {cityHospitals.map((hospital) => <HospitalCard hospital={hospital} />)}
    <hr />
  </>
))}

---

<GlassCard>

**Как пользоваться этой страницей**

1. Используйте её как отправную точку для самостоятельной проверки.
2. Уточняйте отделение, наличие врача и правила записи напрямую в учреждении.
3. Сочетайте доступ к медицинской помощи с планированием исходных лабораторных анализов и осведомлённостью о неотложных ситуациях.

</GlassCard>

<DangerBox title="Замечание о неотложной помощи">
Этот справочник не является службой экстренной маршрутизации пациентов. При острых симптомах сначала обращайтесь в неотложную помощь, а не полагайтесь на просмотр справочника.
</DangerBox>
