---
title: "Direktori Medis"
description: "Direktori terstruktur berisi sumber daya akses layanan yang dilaporkan komunitas. Pastikan untuk memverifikasi langsung sebelum Anda berangkat."
evidenceLevel: X
lastReviewed: 2026-04-09
references: []
pagefind: false
---

import HospitalCard from '../../../../components/ui/HospitalCard.astro';
import WarningBox from '../../../../components/ui/WarningBox.astro';
import DangerBox from '../../../../components/ui/DangerBox.astro';
import GlassCard from '../../../../components/ui/GlassCard.astro';

import hospitalsRaw from '../../../../data/hospitals.json';

export const hospitals = hospitalsRaw;
export const cityOrder = ['北京', '上海', '广州', '深圳', '杭州', '南京', '武汉', '成都', '重庆', '西安', '沈阳', '香港', '台北'];
export const hospitalsByCity = cityOrder
  .map((city) => ({
    city,
    hospitals: hospitals.filter((hospital) => hospital.city === city),
  }))
  .filter(({ hospitals: cityHospitals }) => cityHospitals.length > 0);
export const cityCount = hospitalsByCity.length;
export const shownCount = hospitalsByCity.reduce((n, group) => n + group.hospitals.length, 0);

<WarningBox title="Verifikasi dulu sebelum berangkat">
Daftar ini merupakan catatan akses yang dikelola bersama oleh komunitas, bukan jaminan resmi dari pihak rumah sakit. Kebijakan, departemen, maupun jadwal dokter bisa berubah sewaktu-waktu, jadi konfirmasi langsung tetap selalu diperlukan.
</WarningBox>

Saat ini direktori ini memuat {shownCount} entri yang dilaporkan komunitas dari {cityCount} kota. Data sumber yang menjadi dasarnya untuk sekarang masih sebagian besar berbahasa Tionghoa, sehingga nama rumah sakit dan catatan setempat mungkin masih tampil dalam bahasa Tionghoa.

---

{hospitalsByCity.map(({ city, hospitals: cityHospitals }) => (
  <>
    <h2>{city}</h2>
    {cityHospitals.map((hospital) => <HospitalCard hospital={hospital} />)}
    <hr />
  </>
))}

---

<GlassCard>

**Cara memakai halaman ini**

1. Jadikan ini sebagai titik awal untuk verifikasi Anda sendiri.
2. Konfirmasikan departemen, jadwal dokter, dan aturan pendaftaran langsung ke rumah sakit yang bersangkutan.
3. Padukan akses ke penyedia layanan dengan perencanaan pemeriksaan laboratorium dasar dan kesiapan menghadapi keadaan darurat.

</GlassCard>

<DangerBox title="Catatan darurat">
Direktori ini bukan layanan rujukan darurat. Jika Anda mengalami gejala yang mendesak, segera pergi ke layanan gawat darurat alih-alih mengandalkan penelusuran direktori.
</DangerBox>
