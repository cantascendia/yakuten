---
title: "Direktoryo ng mga Pasilidad na Medikal"
description: "Isang maayos na direktoryo ng mga mapagkukunan para sa pag-access sa pangangalaga na iniulat ng komunidad. Tiyakin nang direkta bago ka pumunta."
evidenceLevel: X
lastReviewed: 2026-04-09
references: []
pagefind: false
---

import HospitalCard from '../../../../components/ui/HospitalCard.astro';
import WarningBox from '../../../../components/ui/WarningBox.astro';
import DangerBox from '../../../../components/ui/DangerBox.astro';
import GlassCard from '../../../../components/ui/GlassCard.astro';

import hospitalsRaw from '../../../../data/hospitals.json';

export const hospitals = hospitalsRaw;
export const cityOrder = ['北京', '上海', '广州', '深圳', '杭州', '南京', '武汉', '成都', '重庆', '西安', '沈阳', '香港', '台北'];
export const hospitalsByCity = cityOrder
  .map((city) => ({
    city,
    hospitals: hospitals.filter((hospital) => hospital.city === city),
  }))
  .filter(({ hospitals: cityHospitals }) => cityHospitals.length > 0);
export const cityCount = hospitalsByCity.length;
export const shownCount = hospitalsByCity.reduce((n, group) => n + group.hospitals.length, 0);

<WarningBox title="Tiyakin bago ka pumunta">
Ang mga listahang ito ay mga tala sa pag-access na pinangangalagaan ng komunidad, hindi opisyal na garantiya mula sa mga institusyon. Mabilis na nagbabago ang mga patakaran, departamento, at availability ng mga doktor, kaya laging kailangang kumpirmahin ito nang direkta.
</WarningBox>

Sa ngayon, naglalaman ang direktoryong ito ng {shownCount} na entry na iniulat ng komunidad sa {cityCount} na lungsod. Pangunahing nasa Tsino pa rin ang pinagmumulang datos sa yugtong ito, kaya maaaring lumitaw pa rin sa Tsino ang mga pangalan ng ospital at ang mga lokal na tala.

---

{hospitalsByCity.map(({ city, hospitals: cityHospitals }) => (
  <>
    <h2>{city}</h2>
    {cityHospitals.map((hospital) => <HospitalCard hospital={hospital} />)}
    <hr />
  </>
))}

---

<GlassCard>

**Paano gamitin ang pahinang ito**

1. Gamitin ito bilang panimulang punto para sa sarili mong pag-verify.
2. Direktang kumpirmahin sa institusyon ang departamento, availability ng doktor, at mga panuntunan sa pagpapatala.
3. Isabay ang pag-access sa provider sa pagpaplano ng baseline na lab at sa kamalayan sa emerhensiya.

</GlassCard>

<DangerBox title="Tala sa emerhensiya">
Ang direktoryong ito ay hindi isang serbisyo ng referral para sa emerhensiya. Kung may madaliang sintomas ka, magtungo muna sa emergency care sa halip na umasa sa pag-browse ng direktoryo.
</DangerBox>
