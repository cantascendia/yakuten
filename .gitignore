# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Editor files
.idea/
.vscode/

# Logs
*.log

# Dependencies
node_modules/

# Build output
dist/

# Astro
.astro/

# Environment
.env
.env.*

# Vercel
.vercel/

# Stitch exports (design artifacts, not source)
stitch-exports/
stitch-prompts.md
*.zip

# Claude Design extracted artifacts
.design-unzip/
.claude-design-bundle/

# Claude Code temporary agent worktrees (never commit — embedded git checkouts)
.claude/worktrees/

.vercel
.env*.local

# SEO automation secrets & raw pulls (do not commit)
.gsc-credentials.json
docs/data/gsc-*.csv
# AI SEO report — derived from GSC, may contain real search queries
docs/data/ai-seo-report-*.md
# keyword-gap 快照含 GSC 真实查询词 + Trends 数据 — 公开仓库不存 SEO 数据（改用 Claude Code 手动 SEO）
docs/seo-keyword-gap.md
docs/data/trends-latest.json
docs/data/trends-20*.json
# 保留 docs/data/trends-keywords.json（手动种子配置，非性能数据）

# gpt-image-2 raw downloads (large PNG/JPG source, compressed webp committed separately)
scripts/gpt-image-2-raw/

# Diagram source masters (deployed WebP versions live under public/images/diagrams/{locale}/;
# raw PNG masters are not part of the production CDN — recoverable from git history
# (commits 1dd0af4 / 624172f / a58dfd5 / a722710 / 9c64618 etc.) if re-processing needed.)
public/images/diagrams/*/source/

# Playwright runtime artifacts
test-results/
playwright-report/

# CTO harness 升级备份（本地留存，不入库）
.claude/*.bak-*/

# 降质化验单素材：可由 scripts/degrade-lab-fixtures.mjs 从合成图确定性重建，
# 15 张 ~2MB 不必进仓库（合成源图 tests/fixtures/lab/IMG-*.jpg 是跟踪的）
tests/fixtures/lab/degraded/
